# Cloned third-party source — untracked. Re-fetch with:
#   mkdir -p refs && cd refs
#   git clone https://github.com/rayonlabs/chutes.git        && (cd chutes        && git checkout 08d79872)
#   git clone https://github.com/rayonlabs/chutes-api.git    && (cd chutes-api    && git checkout 77b6f355)
#   git clone https://github.com/rayonlabs/chutes-miner.git  && (cd chutes-miner  && git checkout 7afea4b1)
refs/

# Live API key — never commit
verify/ck
verify/*.key
